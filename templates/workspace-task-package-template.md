# Workspace Task Package Template

状态：长期模板
维护窗口：ControlWorkspace

本模板用于 `.workspace-active/workspace/current/` 中的 wave 执行计划或验收后的下一波计划。任务包不是把 TODO 简单堆在一起，而是把当前真实阻塞点之前，同阶段、同窗口、同边界、同验证链路下可以安全推进的主线动作和可关闭 TODO 合并成一组可验收任务。

脚本锚点和当前计划外层格式以 `scripts/README.md` 的脚本可读格式说明为准；新建当前计划优先从 `templates/workspace-control-plan-template.md` 开始，再把本模板的任务包详情填入 `## 任务包`。

## 使用前检查

- 当前任务包对应的阶段：
- 当前北京时间（YYYY-MM-DD HH:mm CST）：
- 下一处真实阻塞点：
- 阻塞点之前还能安全推进的事项：
- 本包是否共享同一窗口 / 边界 / 验证链路：
- 是否存在多种解释 / 设计分叉；是否已向用户确认：
- 本包如何把模糊任务转成可验证目标：
- 代码改动追溯范围和 orphan 清理边界：
- 执行窗口是否必须先读取目标仓库 `AGENTS.md` 并明确自身定位：
- 本包是否足够大，允许执行窗口自行判断启用 Codex 子 agent：
- 不打包的事项及原因：

## 任务包

任务包面向开发者阅读时，默认使用“窄索引 + 单包详情”。不要把所有字段塞进一张横向大表。

- 下一处真实阻塞点：
- 阻塞点之前还能做：

| 任务包 ID | 窗口 | 摘要 | 状态 |
| --- | --- | --- | --- |
| PKG-1 | `Repo` |  | 待启动 |

## 派发时序（北京时间 UTC+8）

| 北京时间 | 类型 | 窗口 / 对象 | 事件 | 发送名单 / 状态 |
| --- | --- | --- | --- | --- |
| YYYY-MM-DD HH:mm CST | 派发 | `Repo` | 发送 PKG-1。 | `Repo` 待启动 / 执行中 |

### PKG-1：<任务包名称>

窗口：`Repo`

派发时间（北京时间，YYYY-MM-DD HH:mm CST）：

状态更新时间（北京时间，YYYY-MM-DD HH:mm CST）：

当前阶段目标：

-

可验证目标：

-

主线动作：

-

可一起关闭的 TODO：

- `TODO-ID`：

明确不包含：

-

下一处真实阻塞点：

-

阻塞点之前还能做：

-

文件 / 模块边界：

- 允许：
- 禁止：
- orphan 清理：只清理由本次改动产生的 orphan；无关 dead code 先记录，不直接删除。

执行前置硬规则：

- 先读取本 workspace `AGENTS.md`、当前总控文档和目标仓库自己的 `AGENTS.md`。
- 开始执行前先明确声明当前窗口定位、目标仓库职责、本轮任务职责，以及本仓库明确不承担的职责。
- 如果无法确认目标仓库定位，先停下回填阻塞，不继续扫描、改文档或写代码。
- 若本任务包较大，可在当前窗口职责和计划边界内自行判断是否开启 Codex 子 agent 分担工作；子 agent 不能跨窗口代领，最终由当前窗口统一复核和回填。

阻塞 / 依赖：

-

统一验证命令：

```text
<commands>
```

回填要求：

- 回填时间（北京时间，YYYY-MM-DD HH:mm CST）：
- 完成范围：
- 文件 / 模块变化：
- 提交 hash：
- 验证命令和结果：
- 已关闭 TODO 证据：
- 未关闭 TODO 去向：
- 遗留风险：
- 下一步建议：

## 反模式

- 不要为了“显得充分”把职责边界不同、验证路径不同或上游未解开的 TODO 塞进同一包。
- 不要遇到未来阻塞就只派阻塞前的最小一小步；先确认阻塞点之前还能做什么。
- 不要把分析发现留在聊天里；未当轮解决的问题必须入 TODO 或说明不入账理由。
- 不要为未证实场景新增 fallback、防护或错误处理；需要保留兼容时写清真实消费方和清理条件。
- 不要把任务包写成一张大杂烩；每个包都必须有统一完成定义和统一验证路径。
- 不要省略 `AGENTS.md` 读取和窗口定位声明；这两项是派发硬规则，不是建议。
